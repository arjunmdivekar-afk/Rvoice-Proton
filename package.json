{
  "name": "rvoice-proton",
  "version": "1.0.0",
  "description": "Next-Generation Voice AI Assistant & Meeting Intelligence Platform powered by LM Studio",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "server": "npm run dev --workspace=server",
    "client": "npm run dev --workspace=client"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "author": "Arjun Divekar",
  "license": "MIT"
}
